# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

.claude

# Secrets & signing — never commit
.env
.env.*
!.env.example
*.pem
*.key
*.p12
*.pfx
*.cer
*.crt
*.mobileprovision
*.keystore
*.jks
secrets.*
# Tauri updater signing key — NEVER commit the private key (.key);
# the public .key.pub is safe to commit.
*.key.enc
.tauri/
src-tauri/.tauri/
# Apple notarization / signing creds
*.p8
# Sentry / analytics tokens that some setups drop as dotfiles
.sentryclirc

# Build output (Rust)
src-tauri/target/

# Bun
bun.lockb.*

AGENTS.md
CLAUDE.md
INSTRUCTIONS.md
todo.txt